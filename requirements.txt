anthropic>=0.40.0
click>=8.0
requests>=2.31
python-dotenv>=1.0
rich>=13.0
beautifulsoup4>=4.12
playwright>=1.40
pypdf>=4.0
pillow>=10.0

# Optional — uncomment to enable the Firecrawl fallback scraper (Tier 2.5).
# Also requires FIRECRAWL_API_KEY in your environment. The tool runs fine
# without this package; a conditional import handles its absence silently.
# firecrawl-py>=1.0
